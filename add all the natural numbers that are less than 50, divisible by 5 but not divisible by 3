#Write a program to add all the natural numbers that are less than 50, divisible by 5 but not divisible by 3.

#Initially
total=0

#create a list of numbers from 1 to 50
nums=list(range(1,50))

#Iterate the list using for loop to find number divisible by 5 but not divisible by 3
for num in nums:
    if num%5==0:
        if num%3==0: continue
        else:
            total=total+num
    else: continue
print('Addition of all the natural numbers that are less than 50, divisible by 5 but not divisible by 3 is', total)
